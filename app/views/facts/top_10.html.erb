

<div class="top-facts">
    <% @facts.each_with_index do |fact, index| %>
    <div class="card">
        <div class="photo">
            <%= image_tag fact.image.url,  class:"card-img-top" %>
            </div>
            <div class="card-block">
            <h4 class="card-title">Rank <%= index + 1 %><br><em>(<%= pluralize(fact.vote.count, 'vote') %>)</em></h4>
            <p class="card-text">
                <%= fact.fact %>
            </p>
            <!-- Vote button -->
            <%= form_with model: fact, method: :patch do |f| %>
            <%= f.hidden_field :vote %>
            <% end %>
        </div>
    </div>
    <br>
    <% end %>

    <div class="back-to-top">
        <a href="#">Back to Top</a>
    </div>
</div>

